# OpenAI API key used by the AI SDK orchestrator (Phase 2+).
# Get one at https://platform.openai.com/api-keys
OPENAI_API_KEY=

# Optional override for the SQLite file path (defaults to ./local.db).
# Tests use `:memory:`; Playwright uses ./local-e2e.db.
# DATABASE_FILE=./local.db
